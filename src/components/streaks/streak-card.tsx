export default async function StreakCard(userId: string) {
  return (
    <div className="streak-card">
      <h2>Streak Card for User {userId}</h2>
      <p>This is a placeholder for the streak card content.</p>
    </div>
  );
}
